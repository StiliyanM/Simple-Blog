@model IEnumerable<CommentListingViewModel>
@foreach (var comment in Model)
{
<div class="row">
    <div class="col-sm-10">
        <h4>@comment.Author <small>@comment.PublishDate.ToString("f")</small></h4>
        <p>
            @comment.Content
        </p>
    </div>
</div>
}
